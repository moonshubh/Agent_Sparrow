# Core Application Dependencies (Minimal for faster builds)
fastapi==0.115.5
uvicorn==0.29.0
python-multipart>=0.0.6
pydantic==2.11.5
pydantic-settings
python-dotenv

# Essential LLM/Agent Dependencies
langchain==0.3.25
langchain-core==0.3.65
langchain-google-genai==2.1.5
langgraph

# Database
supabase>=2.0.0
psycopg2-binary>=2.9.0
pgvector

# External APIs
tavily-python==0.3.0

# Redis for caching
redis==4.6.0

# Essential utilities
tiktoken
httpx>=0.25.0
aiohttp
requests

# Minimal OpenTelemetry (comment out if build still times out)
# opentelemetry-api
# opentelemetry-sdk